#include <stdio.h>

main (){

    int x = 4;
    int y = 5;

    int z = x + y; //This is the same as 4 + 5, just that in this case am using the variables to make the operation

    printf("%d", z);
}