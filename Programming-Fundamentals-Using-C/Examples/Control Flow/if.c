#include <stdio.h>

int main ()
{
    printf("DISCO PARTY DOOR");

    int age;

    printf("what is your age: ");
    scanf("%d", &age);

    if(age >= 18)
    {
        printf("You are allowed to enter");
    }
    
}